The reward calculation uses:
Reward = (Current Time - Staking Start Time) * (Reward Rate / 3600)

Note: Ensure the RewardToken contract has been pre-funded with enough tokens to cover 
all potential claims, or grant the NFTStaking contract 'Mint' permissions on the token.
